/*
** How would you test the word count program? What kinds of input are most
** likely to uncover bugs if there are any?
*/

/*
** In order to test the word count program, we could only input empty lines or
** make no input at all. To improve the program we could also make sure that the
** newline character '\n' is excluded from the character count.
*/

int main(void) {
  return 0;
}
